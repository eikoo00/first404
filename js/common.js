$('.container').infiniteScroll({
    path: '.pagination__next',
    append: '.post',
    history: false,
    hideNav: '.pagination',
  });

// $(function(){ // if document is ready
//   alert('jQuery is ready.')
// });
